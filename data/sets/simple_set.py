#The first function should be named observed and should have no parameters. 
def observed():
  #I created a set named observations. 
  observations = {"Flying Car", "Sky Scraper", "Laser", "Dome"}
  #function should return the set observations. 
  return observations
#second function
def run():
  #will print first function
  print(observed())
#call the function run will actually will print the first function's set
run()
